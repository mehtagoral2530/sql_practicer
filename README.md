# sql_practicer

## Cursor agents

Repo rules pre-approve common dev/git commands. To reduce IDE sandbox prompts, enable **Auto-Run** in Cursor Settings → Agents. See [`.cursor/AGENTS.md`](.cursor/AGENTS.md).
